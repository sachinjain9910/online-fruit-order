function sendSMS(userId, message) {
  // external integration would go here
}

function sendEmail(userId, message) {
  // external integration would go here
}

module.exports = {
  sendSMS,
  sendEmail
};